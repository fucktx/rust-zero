[package]
name = "core"
version = "0.1.0"
edition = "2024"
description = "rust-zero 基础核心库（被 rest 等 crate 复用）"
license = "Apache-2.0"
repository = "https://github.com/fucktx/rust-zero"
readme = "../README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
